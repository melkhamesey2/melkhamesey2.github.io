{
  "name": "gk-development-product-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run validate && astro build && node scripts/validate-build.mjs",
    "validate:build": "node scripts/validate-build.mjs",
    "check": "astro check",
    "validate": "node scripts/validate-content.mjs",
    "preview": "astro preview"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "astro": "^7.3.3",
    "typescript": "^5.9.2"
  }
}
